---
description: Project Development Guidelines
globs: ["**/*"]
alwaysApply: true
---

# spec-kit-test Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-03-04

## Active Technologies
- No new application code; tooling may use shell or existing stack (Go/Node for scripts if needed). + Docker (and Docker Compose) for container lifecycle; MongoDB official image. (002-containerize-database)
- MongoDB remains the primary data store (constitution); this feature adds a containerized way to run it. (002-containerize-database)
- Go 1.23+ (existing backend); no new application code; container definition and tooling only. + Docker, Docker Compose; existing Go backend (standard library, MongoDB driver). (003-containerize-backend)
- MongoDB (unchanged; backend uses MONGODB_URI; in Compose set to `mongodb://db:27017`). (003-containerize-backend)

- Go 1.21+ (backend), TypeScript 5.x (frontend) + React 18, Material UI (MUI), Go standard library + MongoDB driver; frontend build (e.g. Vite) (001-issue-crud)

## Project Structure

```text
backend/
frontend/
tests/
```

## Commands

npm test && npm run lint

## Code Style

Go 1.21+ (backend), TypeScript 5.x (frontend): Follow standard conventions

## Recent Changes
- 003-containerize-backend: Added Go 1.23+ (existing backend); no new application code; container definition and tooling only. + Docker, Docker Compose; existing Go backend (standard library, MongoDB driver).
- 002-containerize-database: Added No new application code; tooling may use shell or existing stack (Go/Node for scripts if needed). + Docker (and Docker Compose) for container lifecycle; MongoDB official image.

- 001-issue-crud: Added Go 1.21+ (backend), TypeScript 5.x (frontend) + React 18, Material UI (MUI), Go standard library + MongoDB driver; frontend build (e.g. Vite)

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
